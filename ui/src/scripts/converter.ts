const Converter = {
  toMoney(val: number): string {
    return "";
  },

  toSUI(val: number, decimals: number = 8): bigint {
    return BigInt(0);
  },

  fromSUI(val: bigint): number {
    return 0;
  },
};

export { Converter };
